package com.example.appmovil.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de Colores de HuertoHogar

val SoftWhite = Color(0xFFF7F7F7)       // Fondo principal
val EmeraldGreen = Color(0xFF2E8B57)    // Acento 1: Botones, Enlaces (Frescura)
val MustardYellow = Color(0xFFFFD700)   // Acento 2: Ofertas, Promociones
val LightBrown = Color(0xFF8B4513)      // Títulos, Subtítulos (Rusticidad)

val DarkGray = Color(0xFF333333)        // Texto principal
val MediumGray = Color(0xFF666666)      // Texto secundario

// Colores del tema (adaptados a la paleta de HuertoHogar)
val Primary = EmeraldGreen
val OnPrimary = Color.White
val Secondary = MustardYellow
val Background = SoftWhite
val Surface = Color.White
val OnBackground = DarkGray
val OnSurface = DarkGray

// Puedes añadir más si usas Light y Dark Theme
// val PrimaryDark = Color(0xFF1B5E20)
// val BackgroundDark = Color(0xFF121212)